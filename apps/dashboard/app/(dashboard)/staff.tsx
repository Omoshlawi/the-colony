import { StyleSheet, Text, View } from "react-native";
import React from "react";
import { StaffScreen } from "@colony/features-dashboard";

const StaffRoles = () => {
  return <StaffScreen />;
};

export default StaffRoles;

const styles = StyleSheet.create({});

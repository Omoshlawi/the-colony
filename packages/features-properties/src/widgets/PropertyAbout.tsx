import { StyleSheet, Text, View } from "react-native";
import React from "react";

const PropertyAbout = () => {
  return (
    <View>
      <Text>PropertyAbout</Text>
    </View>
  );
};

export default PropertyAbout;

const styles = StyleSheet.create({});

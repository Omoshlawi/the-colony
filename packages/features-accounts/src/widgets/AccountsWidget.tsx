import { StyleSheet, Text, View } from "react-native";

export const AccountWidget = () => (
  <View>
    <Text>account widget</Text>
  </View>
);

const styles = StyleSheet.create({
  firstListItem: {
    marginBottom: 16,
  },
});

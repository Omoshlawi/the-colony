import { StyleSheet, Text, View } from "react-native";
import React from "react";

const LeaseListingFormSection = () => {
  return (
    <View>
      <Text>LeaseListingFormSection</Text>
    </View>
  );
};

export default LeaseListingFormSection;

const styles = StyleSheet.create({});
